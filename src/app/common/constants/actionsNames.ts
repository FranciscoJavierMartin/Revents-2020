export enum eventActionsName {
  FETCH_EVENTS,
  CREATE_EVENT,
  UPDATE_EVENT,
  DELETE_EVENT,
}

export enum authActionName {
  SIGN_IN_USER,
  SIGN_OUT_USER,
}

export enum asyncActionName {
  START,
  FINISH,
  ERROR
}