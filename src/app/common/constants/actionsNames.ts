export enum eventActionsName {
  CREATE_EVENT,
  UPDATE_EVENT,
  DELETE_EVENT,
}

export enum modalActionName {
  OPEN_MODAL,
  CLOSE_MODAL,
}

export enum authActionName {
  SIGN_IN_USER,
  SIGN_OUT_USER,
}
